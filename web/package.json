{
  "name": "@cube-practice/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "preview": "pnpm build && wrangler dev",
    "deploy": "pnpm build && pnpm deploy:built",
    "deploy:built": "wrangler deploy",
    "deploy:dry-run": "pnpm build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@cube-practice/scramble": "workspace:*",
    "@cube-practice/timer-domain": "workspace:*",
    "next": "16.3.1",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "tailwindcss": "^4",
    "typescript": "7.0.2",
    "wrangler": "^4.123.0"
  }
}
